dotNetFx40_Full_x86_x64# .Net 4.0 for viruses who doesn't work on old .Net Frameworks (-_-)
ImmunityDebugger_1_85_setup# since we need to deploy it on agent
inetsim-1.2.5.tar.gz# for linux agent or we can just apt-get it instead
modreg.bat# generated by vbox/tools/HideVBox/camouflage.py to modify registry on agent for hiding VBox presence
pafish.exe# in case we need to check if camouflage works okayish
NDP451-KB2858728-x86-x64-AllOS-ENU# seems it's .Net 4.5.1
mona.py# from Corelan GCV (look @ docs/copyright.txt) # we need it to parse IAT and set breakpoints by mask
pywin32-217.win32-py2.7.exe# good to have since we need to actually simulate clicks and text input by keyboard into Imm
sdl-apatedns.zip# to deploy on Windows agent so we can redirect all DNS requests to Linux agent